package top5;

import input.commands.CommandIn;

public class getTop {
    private CommandIn cmd;

    public getTop(final CommandIn cmd) {
        this.cmd = cmd;
    }

    public CommandIn getCmd() {
        return cmd;
    }
}
